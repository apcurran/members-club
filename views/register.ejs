<%- include("./partials/navbar") %>
<div class="container">

    <h2>Create Account</h2>
    <% if (typeof errors !== "undefined") { %>
        <% errors.forEach(error => { %>
            <p class="error"><%= error.msg %> <%= error.param %></p>
        <% }); %> 
    <% } %>
    <% if (typeof emailAlreadyExists !== "undefined") { %>
        <p class="error"><%= emailAlreadyExists %></p>
    <% } %>  
    <form action="/register" method="POST" class="form-register">
        <div class="form-group">
            <label for="email" class="form-group__label">Email</label>
            <input type="email" class="form-group__input" name="email" value="<%= (typeof email === "undefined" ? "" : email) %> " required>
        </div>    
        <div class="form-group">
            <label for="password" class="form-group__label">Password</label>
            <input type="password" class="form-group__input" name="password" required>
        </div>
        <div class="form-group">
            <input type="submit" value="Submit" class="form-group__submit">
        </div>  
    </form>

</div>
<%- include("./partials/footer") %> 